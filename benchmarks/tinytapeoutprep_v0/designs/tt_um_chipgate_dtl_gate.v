// TinyTapeout wrapper for ChipGate DTL safety gate
// Generated by ChipGate TinyTapeoutPrep
// This is a public demonstration design. It does not prove
// silicon correctness, fabrication readiness, or physical safety.

module tt_um_chipgate_dtl_gate (
    input  wire clk,
    input  wire rst_n,
    input  wire [7:0] ui_in,
    output wire [7:0] uo_out,
    input  wire [7:0] uio_in,
    output wire [7:0] uio_out,
    output wire [7:0] uio_oe,
    input  wire ena
);

    // Core signal wires
    wire ai_output;
    wire verifier_ok;
    wire policy_ok;
    wire sensor_ok;
    wire timeout;
    wire kill_switch;
    wire reset;
    wire actuator_enable;
    wire blocked;
    wire failsafe;
    wire approved;
    wire evidence_pulse;

    // Input pin mapping
    assign ai_output = ui_in[0];
    assign verifier_ok = ui_in[1];
    assign policy_ok = ui_in[2];
    assign sensor_ok = ui_in[3];
    assign timeout = ui_in[4];
    assign kill_switch = ui_in[5];
    assign reset = ui_in[6];

    // Core module instantiation
    tiny_dtl_gate core (
        .ai_output(ai_output),
        .verifier_ok(verifier_ok),
        .policy_ok(policy_ok),
        .sensor_ok(sensor_ok),
        .timeout(timeout),
        .kill_switch(kill_switch),
        .reset(reset),
        .actuator_enable(actuator_enable),
        .blocked(blocked),
        .failsafe(failsafe),
        .approved(approved),
        .evidence_pulse(evidence_pulse)
    );

    // Output pin mapping
    assign uo_out[0] = actuator_enable;
    assign uo_out[1] = blocked;
    assign uo_out[2] = failsafe;
    assign uo_out[3] = approved;
    assign uo_out[4] = evidence_pulse;
    assign uo_out[5] = 1'b0;
    assign uo_out[6] = 1'b0;
    assign uo_out[7] = 1'b0;

    // Unused bidirectional pins
    assign uio_out = 8'b0;
    assign uio_oe  = 8'b0;

endmodule